  {
    "schema":[
    {
      "key": "firstName",
      "type": "input",
      "defaultValue": "This is a default value",
      "templateOptions": {
        "label": "First Name (initialized via default value)"
      }
    },
    {
      "key": "lastName",
      "type": "input",
      "defaultValue": "This is a default value",
      "templateOptions": {
                  "type": "password",
        "label": "Last Name (initialized via the model)",
        "required":true
      }
    },
    {
      "key": "view",
      "type": "select",
      "defaultValue": "milky_way",
      "templateOptions": {
        "label": "Select the main view to export to Magento",
        "source":"view",
        "options": [
          { "value": "es_es", "label": "Spain" },
          { "value": "fr_fr", "label": "France" },
          { "value": "en_en", "label": "English" }
        ]
      }
    },
    {
      "key": "agree",
      "type": "checkbox",
      "templateOptions": {
        "label": "Agree? (not initialized at all)",
        "required": true
      }
    }
  ]
  }